# Cowave 대시보드 — 카테고리 담당자 세팅 가이드

에누리 GMV RAWDATA 파일을 기반으로 카테고리 매출 현황(YoY·MoM·쇼핑몰 비교 등)을 한눈에 볼 수 있는 대시보드를 **내 카테고리에 맞게** 빠르게 세팅하기 위한 가이드입니다. 아래 순서대로 따라오면 코딩 경험 없이도 Claude(AI)가 카테고리 코드 설정·데이터 연결까지 자동으로 처리해 줍니다.

---

## 사전 준비

| 항목 | 설치 방법 | 비고 |
|------|-----------|------|
| **Node.js 20+** | https://nodejs.org → LTS 버전 다운로드 | 설치 후 `node -v` 로 확인 |
| **pnpm** | 터미널에서 `npm install -g pnpm` | Node.js 설치 후 실행 |
| **Git** | https://git-scm.com | 윈도우는 Git Bash 포함 |
| **Claude Code** | https://claude.ai/code → 앱 다운로드 | 카테고리 설정 자동화에 사용 |
| **Vercel 계정** | https://vercel.com (GitHub 계정으로 가입) | 온라인 공유 시 필요, 선택 사항 |

---

## 1단계: 프로젝트 클론 & 설치

터미널(또는 Git Bash)을 열고 아래 명령어를 순서대로 실행합니다.

```bash
git clone https://github.com/may1018-git/cowave.git
cd cowave
pnpm install
```

설치가 완료되면 `cowave` 폴더가 생성됩니다.

---

## 2단계: 데이터 폴더 준비

에누리에서 다운로드한 GMV RAWDATA 파일을 아래 구조에 맞게 넣어주세요.

```
cowave/
└── data/
    └── 에누리/
        ├── 가전(2001)GMV_RAWDATA_2025-05-01_2025-05-31.xls
        ├── 가전(2001)GMV_RAWDATA_2025-06-01_2025-06-30.xls
        └── ...
```

**파일 이름 규칙:**

```
{카테고리명}({카테고리코드})GMV_RAWDATA_{시작일}_{종료일}.xls
```

- `{카테고리코드}` = 에누리 중분류 코드 (4자리, 예: `2001`)
- 날짜 형식: `YYYY-MM-DD` 또는 `YYYYMMDD` 모두 지원
- 확장자: `.xls`, `.xlsx` 모두 지원
- 파일이 여러 달이면 월별로 각각 넣으면 됨 (자동 합산)

> **Tip:** `data` 폴더를 OneDrive나 공유 드라이브에 두고 여러 명이 같은 폴더를 바라보게 할 수도 있어요.

---

## 3단계: 환경변수 설정

`cowave` 폴더 안에 `.env.local` 파일을 새로 만들고 아래 한 줄을 입력합니다.

```
DATA_DIR=./data
```

데이터 폴더가 다른 위치에 있다면 절대 경로로 지정하세요.

```
DATA_DIR=C:\Users\홍길동\OneDrive\Cowave_data
```

---

## 4단계: Claude Code로 카테고리 설정 자동화

> Claude Code 앱을 열고, `cowave` 폴더를 프로젝트로 열어주세요.

아래 **프롬프트를 복사**해서 Claude Code 채팅창에 붙여넣습니다.
`[대괄호]` 안의 내용만 내 카테고리에 맞게 수정하면 됩니다.

---

### 📋 Claude에게 붙여넣을 프롬프트

```
안녕, 나는 에누리 [대분류명 — 예: 가전] 카테고리 담당자야.
이 Cowave 대시보드(Next.js 프로젝트)를 내 카테고리 구조에 맞게 수정해줘.

## 내 카테고리 구조

대분류: 코드 [예: 20], 이름 [예: 가전]

중분류 목록:
- 코드 [예: 2001], 이름 [예: TV/영상가전]
- 코드 [예: 2002], 이름 [예: 생활가전]
- 코드 [예: 2003], 이름 [예: 계절가전]
(에누리 카테고리 코드표에서 내 담당 중분류를 모두 작성해)

소분류 목록 (중분류 각각에 대해):

[중분류 2001 / TV/영상가전]
- 코드 [예: 200101], 이름 [예: OLED TV]
- 코드 [예: 200102], 이름 [예: QLED TV]
- 코드 [예: 200103], 이름 [예: 일반 LED TV]

[중분류 2002 / 생활가전]
- 코드 [예: 200201], 이름 [예: 에어컨]
- 코드 [예: 200202], 이름 [예: 냉장고]
- 코드 [예: 200203], 이름 [예: 세탁기]

[중분류 2003 / 계절가전]
- 코드 [예: 200301], 이름 [예: 선풍기]
- 코드 [예: 200302], 이름 [예: 전기히터]

## 수정 요청 사항

1. lib/category-map.ts 를 위 카테고리 구조로 완전히 교체해줘
   (기존 식품 관련 카테고리 코드는 삭제하고 내 카테고리로 대체)
2. 소분류 코드가 없는 중분류가 있다면 일단 중분류만 등록해도 괜찮아
3. 대시보드 코드 어딘가에 "식품" 또는 "라면" 같은 하드코딩된 텍스트가 있으면
   내 카테고리명([대분류명])으로 바꿔줘
4. 수정 완료 후 pnpm exec tsc --noEmit 실행해서 TypeScript 오류 없는지 확인해줘
```

---

Claude가 `lib/category-map.ts` 파일을 자동으로 수정해 줍니다. 완료되면 다음 단계로 이동하세요.

---

## 5단계: 로컬 실행 확인

```bash
pnpm dev
```

브라우저에서 http://localhost:3000 을 열면 대시보드가 실행됩니다.

- 데이터 파일이 있으면 → 실제 데이터로 표시
- 데이터 파일이 없으면 → 샘플(demo) 데이터로 자동 실행 (기능 확인용)

---

## 6단계: (선택) Vercel 배포로 팀과 공유

다른 팀원이 URL로 접근할 수 있게 온라인 배포하는 방법입니다.

### 6-1. GitHub에 코드 올리기

```bash
git add .
git commit -m "내 카테고리 설정 완료"
git push
```

### 6-2. 데이터 파일도 GitHub에 올리기

```bash
git add data/
git commit -m "데이터 파일 추가"
git push
```

> **주의:** 매출 데이터를 GitHub에 올리면 레포가 public일 경우 외부에 공개됩니다. 민감한 데이터라면 레포를 private으로 설정하세요.

### 6-3. Vercel에서 배포

1. https://vercel.com → **Add New Project**
2. GitHub 레포 선택 → **Import**
3. **Environment Variables** 탭 → 추가:
   - Name: `DATA_DIR`
   - Value: `./data`
   - **Sensitive 토글은 OFF** (켜면 값이 저장 안 됨)
4. **Deploy** 클릭

배포 완료 후 `https://내프로젝트.vercel.app` 형태의 URL이 생성됩니다.

---

## 자주 묻는 질문 (FAQ)

**Q: 대시보드가 demo 데이터로만 뜨는데요?**

`DATA_DIR` 환경변수와 파일명 규칙을 확인하세요.
- `.env.local` 파일이 `cowave` 폴더 안에 있는지 확인
- `data/에누리/` 폴더 경로가 맞는지 확인
- 파일명에 `GMV_RAWDATA`가 포함되어 있는지, 날짜 형식이 맞는지 확인
- 서버 재시작: `pnpm dev` 다시 실행

**Q: 쇼핑몰 이름이 이상하게 뜨거나 ID로 표시되는데요?**

에누리 파일의 "쇼핑몰" 컬럼에 있는 ID 값을 확인하세요.
그 다음 Claude에게 아래처럼 요청하면 됩니다:

```
lib/mall-map.ts 에 쇼핑몰을 추가해줘.
내 데이터 파일에서 확인한 쇼핑몰 ID는 다음과 같아:
- ID: 1234, 이름: 롯데온
- ID: 5678, 이름: 위메프
```

**Q: 카테고리 코드를 어디서 확인하나요?**

에누리 판매 관리자 페이지 → 카테고리 분류표, 또는 담당 MD나 운영팀에 요청하세요.
에누리 GMV 파일의 "카테고리" 컬럼에 실제 사용 중인 코드가 포함되어 있기도 합니다.

**Q: 카테고리 소분류가 너무 많은데 다 입력해야 하나요?**

중분류까지만 입력해도 대시보드는 동작합니다. 소분류는 드릴다운(상세 분석) 시 활용되므로, 주요 소분류부터 순차적으로 추가하면 됩니다.

**Q: 기능을 더 추가하거나 바꾸고 싶은데요?**

Claude Code를 열고 원하는 기능을 설명하면 됩니다. 예:

- "매출 데이터를 Excel로 다운로드하는 버튼 추가해줘"
- "상품별 상세 페이지에서 월별 트렌드 차트도 보여줘"
- "쇼핑몰 비교 화면에 수수료 차감 후 순매출도 보여줘"

**Q: Vercel 배포 후 새 데이터 파일을 추가하면?**

`data/에누리/` 폴더에 파일을 추가하고 `git push` 하면 Vercel이 자동으로 재배포합니다.

---

## 지원

문의 사항은 최초 구축자(에누리 식품 카테고리 담당)에게 연락하거나, Claude Code에 질문하면 됩니다.
