# Cowave 대시보드 — 따라하기 가이드

이 가이드는 **개발 경험이 없어도** 따라할 수 있게 작성되었습니다.
모르는 부분이 나오면 그냥 그대로 따라하시고, 막히면 마지막 "도움 요청" 항목을 보세요.

> 💡 한 번 세팅해두면, 그 다음부터는 데이터 파일만 폴더에 넣으면 됩니다.

---

## 🎯 이 가이드로 할 수 있는 것

- 내가 담당하는 카테고리(가전·뷰티·패션 등)의 매출 현황을 한눈에 보는 대시보드
- 에누리 GMV RAWDATA 엑셀 파일만 있으면 자동으로 그래프·표 생성
- YoY/MoM 비교, 쇼핑몰별 매출, 제조사별 매출, 상품 TOP 랭킹
- 본인 PC에서만 보거나, URL로 팀원과 공유 가능

---

## 0단계: 설치할 프로그램 (15분)

이 4가지를 PC에 설치해야 합니다. **순서대로 하나씩** 진행하세요.

### ① Node.js 설치 — 약 3분

대시보드를 실행하는 엔진입니다.

1. https://nodejs.org 접속
2. 큰 초록 버튼 **"LTS"** 클릭 → 설치 파일 다운로드
3. 다운받은 파일 더블클릭 → 그냥 **"Next" 계속 클릭** → 설치 완료
4. 설치 확인:
   - **윈도우**: `Win + R` → `cmd` 입력 → 엔터 → 검은 창이 뜸
   - **맥**: `Cmd + Space` → `터미널` 입력 → 엔터
   - 검은 창에 `node -v` 입력 후 엔터
   - `v20.x.x` 같은 글자가 뜨면 ✅ 성공

### ② pnpm 설치 — 약 1분

위에서 연 검은 창(터미널)에 아래 한 줄을 복사해서 붙여넣고 엔터.

```
npm install -g pnpm
```

설치 완료 후 `pnpm -v` 입력 → 숫자(예: `10.x.x`)가 뜨면 ✅ 성공

### ③ Git 설치 — 약 3분

코드를 받아오는 도구입니다.

1. https://git-scm.com/downloads 접속
2. 본인 OS(Windows/Mac) 클릭 → 다운로드
3. 설치 파일 더블클릭 → **"Next" 계속 클릭** → 설치 완료
   - (윈도우 옵션 화면이 많이 뜨는데 다 기본값으로 두고 Next)
4. 확인: 검은 창(터미널)에 `git --version` 입력 → 숫자가 뜨면 ✅ 성공

### ④ Claude Code 설치 — 약 3분

AI가 직접 코드를 수정해주는 도구입니다. (이게 핵심!)

1. https://claude.ai/code 접속
2. **Download** 클릭 → 본인 OS용 앱 다운로드
3. 설치 후 실행 → Anthropic 계정으로 로그인

> Claude Code 앱이 없으면 이 가이드의 핵심 자동화가 안 됩니다. 꼭 설치하세요.

---

## 1단계: 프로젝트 다운받기 (2분)

터미널을 열고 (위에서 사용한 검은 창) 아래 명령어를 **한 줄씩** 복사·붙여넣기 하세요.

### ① 작업 폴더로 이동

본인의 **바탕화면** 또는 **문서** 폴더로 이동합니다.

**윈도우:**
```
cd %USERPROFILE%\Desktop
```

**맥:**
```
cd ~/Desktop
```

### ② 프로젝트 다운로드

```
git clone https://github.com/may1018-git/cowave.git
```

→ 바탕화면에 `cowave` 폴더가 생깁니다.

### ③ 프로젝트 폴더로 들어가기

```
cd cowave
```

### ④ 필요한 라이브러리 설치 (1~3분 소요)

```
pnpm install
```

설치하는 동안 글자가 쭉 올라가는데, 끝날 때까지 그냥 두세요.

---

## 2단계: 데이터 파일 준비 (5분)

에누리에서 받은 매출 엑셀 파일을 정해진 위치에 넣어야 합니다.

### ① 데이터 폴더 만들기

바탕화면의 `cowave` 폴더를 **파일 탐색기(또는 Finder)** 로 열어주세요.

폴더 안에 새 폴더 두 개를 만듭니다:
- `cowave` 안에 → `data` 폴더 생성
- `data` 안에 → `에누리` 폴더 생성

최종 구조:
```
cowave/
└── data/
    └── 에누리/        ← 여기에 엑셀 파일들을 넣음
```

### ② 엑셀 파일 넣기

에누리에서 받은 파일들을 `data/에누리/` 폴더 안에 **그대로 복사**하세요.

**파일 이름 규칙** (이미 이 형식으로 다운받았을 거예요):

```
{카테고리명}({카테고리코드})GMV_RAWDATA_{시작일}_{종료일}.xls
```

✅ **올바른 예시:**
- `가전(2001)GMV_RAWDATA_2025-05-01_2025-05-31.xls`
- `뷰티(1801)GMV_RAWDATA_2025-06-01_2025-06-30.xls`

❌ **이렇게 바꾸지 마세요** (자동 인식 안 됨):
- `5월 가전 매출.xls`
- `가전_2001_202505.xlsx`

월별로 여러 파일이 있으면 다 같이 넣으면 됩니다 (자동으로 합쳐서 보여줌).

---

## 3단계: 설정 파일 만들기 (1분)

대시보드에게 "데이터 파일이 어디 있는지" 알려줘야 합니다.

1. `cowave` 폴더 안에 **새 파일** 하나 생성
2. 파일 이름: `.env.local` (**점(.)으로 시작**)
3. 파일 내용에 **딱 한 줄** 입력:

```
DATA_DIR=./data
```

4. 저장

> 💡 윈도우 메모장에서 저장할 때 "모든 파일(*.*)" 로 선택하고, 파일 이름에 `.env.local` 그대로 입력하세요. 메모장이 `.txt`를 자동으로 붙이면 안 됩니다.

---

## 4단계: Claude Code로 내 카테고리 설정 (5분) ⭐ 핵심

여기가 가장 중요합니다. **카테고리 코드는 사람마다 다르기 때문에 Claude가 자동으로 수정해 줍니다.**

### ① Claude Code 앱 열기

방금 설치한 Claude Code 앱을 실행하세요.

### ② 프로젝트 열기

- 앱 메뉴에서 **"Open Folder"** 또는 **"프로젝트 열기"** 클릭
- 바탕화면의 `cowave` 폴더 선택

### ③ 프롬프트 붙여넣기

아래 박스 안의 내용을 **통째로 복사**해서 Claude Code 채팅창에 붙여넣으세요.
그리고 `[대괄호]` 안의 내용만 **내 카테고리 정보로 바꿔주세요**.

---

```
안녕, 나는 에누리 [대분류명을 입력 — 예: 가전] 카테고리 담당자야.
이 Cowave 대시보드 프로젝트를 내 카테고리에 맞게 설정해줘.

## 내 담당 카테고리

대분류:
- 코드: [예: 20]
- 이름: [예: 가전]

중분류 (담당하는 모든 중분류를 적어줘):
- 코드 [예: 2001], 이름 [예: TV/영상가전]
- 코드 [예: 2002], 이름 [예: 생활가전]
- 코드 [예: 2003], 이름 [예: 계절가전]

소분류 (아는 것만 적어도 돼. 없으면 이 섹션 통째로 지워도 OK):

[2001 / TV/영상가전 안의 소분류]
- 코드 [예: 200101], 이름 [예: OLED TV]
- 코드 [예: 200102], 이름 [예: QLED TV]

[2002 / 생활가전 안의 소분류]
- 코드 [예: 200201], 이름 [예: 에어컨]
- 코드 [예: 200202], 이름 [예: 냉장고]

## 해줄 일

1. lib/category-map.ts 파일을 위 내용으로 완전히 교체해줘.
   (기존 식품·라면 카테고리는 다 지워도 돼)
2. 대시보드 화면에 "식품"이나 "라면" 같은 글자가 보이면
   내 카테고리명([대분류명])으로 바꿔줘.
3. 다 끝나면 pnpm exec tsc --noEmit 명령어를 실행해서
   오류가 없는지 확인해줘.

내가 모르는 부분(예: 소분류 코드)이 있으면 일단 비워두고 진행해도 돼.
```

---

> 📌 **카테고리 코드를 모르겠다면?**
> - 에누리 판매 관리자 페이지 → 카테고리 분류표 메뉴
> - 또는 본인이 받은 GMV 엑셀 파일을 열어서 "카테고리" 컬럼의 숫자를 확인
> - 또는 운영팀/MD에게 "내 담당 카테고리 코드표 좀 알려주세요" 요청

Claude가 작업을 끝낼 때까지 기다리세요. (보통 1~2분)

---

## 5단계: 대시보드 실행 확인 (1분)

다시 터미널(검은 창)로 돌아와서, 아래 명령어를 입력하세요.

```
pnpm dev
```

`Ready in ...` 같은 글자가 뜨면 ✅ 성공입니다.

브라우저를 열어서 주소창에 입력:
```
http://localhost:3000
```

대시보드가 보이면 끝! 🎉

> 💡 종료할 때는 터미널에서 `Ctrl + C` 누르세요.

---

## 6단계: (선택) 팀원과 URL로 공유하기

본인 PC에서만 보지 말고, 팀원에게 URL을 보내고 싶다면 Vercel에 배포하세요.

### ① GitHub에 코드 올리기

터미널에 차례대로 입력:
```
git add .
git commit -m "내 카테고리 설정 완료"
git push
```

### ② Vercel에서 배포

1. https://vercel.com 접속 → GitHub 계정으로 로그인
2. **"Add New Project"** 클릭
3. 본인의 `cowave` 레포 선택 → **"Import"**
4. **"Environment Variables"** 항목 펼치기:
   - **Name** 칸: `DATA_DIR`
   - **Value** 칸: `./data`
   - **"Sensitive" 토글은 켜지 마세요** (켜면 값이 저장 안 됨)
5. **"Deploy"** 버튼 클릭

2~3분 후 `https://내프로젝트.vercel.app` 같은 URL이 생성됩니다.
이걸 팀원에게 공유하면 끝!

> ⚠️ **주의:** GitHub 레포가 public이면 매출 데이터가 외부에 노출됩니다.
> 민감 데이터라면 GitHub에서 레포를 **Private**으로 설정하세요.
> (레포 페이지 → Settings → 맨 아래 Change visibility)

---

## 😕 문제 해결

### "그냥 demo 데이터만 뜨는데요?"

데이터 파일이 인식 안 됐을 가능성 99%. 체크리스트:

1. ✅ `cowave/data/에누리/` 폴더 안에 파일이 있나요?
2. ✅ 파일 이름이 `XX(코드)GMV_RAWDATA_날짜_날짜.xls` 형식인가요?
3. ✅ `.env.local` 파일이 `cowave` 폴더 바로 안에 있나요?
4. ✅ `.env.local` 내용이 `DATA_DIR=./data` 한 줄인가요?

다 맞다면, 터미널에서 `Ctrl + C` → `pnpm dev` 다시 실행.

### "쇼핑몰 이름이 안 뜨고 숫자만 떠요"

새로운 쇼핑몰 ID가 등록 안 된 거예요. Claude Code에 이렇게 요청:

```
대시보드에 안 뜨는 쇼핑몰이 있어. lib/mall-map.ts 에 추가해줘.
- ID: 1234, 이름: 롯데온
- ID: 5678, 이름: 위메프
```

(ID 확인 방법: 엑셀 파일의 "쇼핑몰" 컬럼 숫자를 보세요)

### "에러 메시지가 떠요 / 모르는 화면이 떠요"

당황하지 말고, **에러 메시지를 그대로 복사**해서 Claude Code에 붙여넣고
"이 에러 해결해줘"라고 하면 됩니다. 거의 다 해결돼요.

### "기능을 더 추가하고 싶어요"

Claude Code에 자연어로 요청하면 됩니다. 예시:

- "매출 데이터를 Excel로 다운받는 버튼 추가해줘"
- "월별 매출 트렌드 차트를 메인 페이지에도 보여줘"
- "쇼핑몰별로 수수료 차감 후 순매출도 표시해줘"

### "Vercel 배포 후에 새 데이터 파일을 추가하려면?"

`data/에누리/` 폴더에 새 파일을 넣고:
```
git add data/
git commit -m "5월 데이터 추가"
git push
```

→ Vercel이 자동으로 재배포합니다 (2~3분).

---

## 📞 도움 요청

- **사용 중 막히는 부분**: Claude Code 채팅창에 그냥 물어보세요. 가장 빠릅니다.
- **카테고리 코드 관련**: 운영팀 또는 담당 MD에게 문의
- **최초 구축자**: 에누리 식품 카테고리 담당자
